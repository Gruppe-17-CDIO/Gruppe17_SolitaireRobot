import dataObjects.Move;
import dataObjects.SolitaireState;
import java.util.List;

public class Logic implements I_Logic {
    @Override
    public List<Move> getMoves(SolitaireState cards) {
        return null;
    }
}
